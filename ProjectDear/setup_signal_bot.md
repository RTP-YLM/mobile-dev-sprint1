# 📱 Signal Bot Setup Guide

## วิธีการทำงาน
Bot จะ:
1. 📊 ดึงข้อมูล Gold (XAUUSD) จาก Yahoo Finance (ฟรี!)
2. 🧮 วิเคราะห์ตาม RSI, Bollinger Bands, ATR
3. 📱 ส่ง signal BUY/SELL มาทาง Telegram
4. 🖱️ นายเข้าไปเทรดเองบน https://my.exness.com/webtrading/

## Setup ขั้นตอนที่ 1: ติดตั้ง Dependencies

```bash
pip3 install yfinance pandas numpy requests
```

## Setup ขั้นตอนที่ 2: สร้าง Telegram Bot

### 1. หา BotFather บน Telegram
- เปิด Telegram → search `@BotFather`
- พิมพ์: `/newbot`
- ตั้งชื่อ bot (เช่น: "Gold Signal Bot")
- ตั้ง username (เช่น: "my_gold_signal_bot")
- **เก็บ BOT_TOKEN** ที่ได้รับ (เช่น: `123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11`)

### 2. หา Chat ID ของนาย
- เปิด Telegram → search bot ที่เพิ่งสร้าง
- พิมพ์: `/start`
- เปิดเบราว์เซอร์: `https://api.telegram.org/bot<BOT_TOKEN>/getUpdates`
  (แทน `<BOT_TOKEN>` ด้วย token จริง)
- หา `"chat":{"id":123456789}` → **เก็บ CHAT_ID นี้**

## Setup ขั้นตอนที่ 3: Config Bot

แก้ไฟล์ `signal_bot.py` บรรทัด 156-157:

```python
# Optional: Set Telegram credentials
bot.set_telegram("YOUR_BOT_TOKEN", "YOUR_CHAT_ID")
```

แทนด้วยค่าจริง:

```python
bot.set_telegram("123456:ABC-DEF1234ghIkl-zyx57W2v1u123ew11", "123456789")
```

## วิธีรัน Bot

```bash
cd /Users/rtp-ylm/clawd/ProjectDear
python3 signal_bot.py
```

## การทำงาน

Bot จะ:
- ✅ ตรวจสอบตลาดทุก 1 นาที
- ✅ ส่ง signal เมื่อเจอโอกาส (BUY/SELL)
- ✅ ส่งครั้งละ 1 signal ห่างกันอย่างน้อย 15 นาที (ไม่ spam)
- ✅ ทำงานเฉพาะช่วงเวลาที่กำหนด (14:00-02:00)

## ตัวอย่าง Signal Message

```
🟢 BUY SIGNAL - XAUUSD

📊 Market Analysis:
Price: $2738.50
RSI: 32.4
ATR: 2.15

📈 Suggested Trade:
Entry: $2738.50
Stop Loss: $2737.00 (15 pips)
Take Profit: $2741.50 (30 pips)

Risk: 2.0% per trade
Max Trades Today: 5

⏰ 2026-01-29 15:30:00

Execute manually on:
https://my.exness.com/webtrading/
```

## ปรับแต่ง Strategy

แก้ไฟล์ `config.json`:

```json
{
  "strategy": {
    "symbol": "XAUUSD",
    "timeframe": "M5",           // M1, M5, M15, M30, H1, H4, D1
    "risk_percent": 2.0,          // % ของ balance ต่อ 1 trade
    "stop_loss_pips": 15,         // SL (pips)
    "take_profit_pips": 30,       // TP (pips)
    "max_trades_per_day": 5,      // จำกัดจำนวน signal ต่อวัน
    "max_consecutive_losses": 3,
    "trading_hours": {
      "start": 14,                // เริ่ม 14:00
      "end": 2                    // สิ้นสุด 02:00 (วันถัดไป)
    }
  }
}
```

## Troubleshooting

### ไม่ได้รับ signal
- ตรวจสอบ trading hours (14:00-02:00)
- ตรวจสอบว่ามี signal จริงหรือไม่ (ดูใน console)

### Telegram ไม่ส่ง
- ตรวจสอบ BOT_TOKEN และ CHAT_ID ถูกต้อง
- ตรวจสอบว่าเริ่มบทสนทนากับ bot แล้ว (`/start`)

### ข้อมูล Yahoo Finance ผิดเพี้ยน
- Yahoo Finance ใช้ Gold Futures (GC=F) → ราคาอาจต่างจาก spot price เล็กน้อย
- ถ้าต้องการความแม่นยำสูง → ใช้ Exness API (ถ้ามี) หรือ paid data feed

## หยุด Bot

กด `Ctrl+C` ใน terminal

---

✅ **ข้อดี Semi-Auto:**
- ไม่ต้อง Docker/MT5/VPS
- ฟรี (ไม่มีค่า API)
- ควบคุมได้ทุก trade
- เรียนรู้ strategy ได้ดีกว่า

⚠️ **ข้อเสีย:**
- ต้องเข้ามาเทรดเอง (ไม่ auto)
- ข้อมูลจาก Yahoo อาจช้ากว่า real-time
- ต้องเปิด bot อยู่ตลอด (หรือใช้ VPS รัน 24/7)
